/**
 * ScenePromptBuilder
 *
 * Builds the user prompt for scene generation from the user's question.
 * Delegates to lib/storytelling/scene-gen-prompt.ts for the actual prompt text.
 */

import { buildScenePrompt } from '@/lib/storytelling/scene-gen-prompt';

export { buildScenePrompt };
